import tensorflow as tf 

def loadData(path):
	"""
	[trainData, trainLabel, testData, testLabel] = loadData(path)
		Load the minist data
	inputs:
		path	path to the data folder containing the mnist data with original file names

	outputs:

	"""
	pass

if __name__ == "main":
	pass

	